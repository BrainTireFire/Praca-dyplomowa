


export type Effect = {
};

export const initialState: Effect = {
};

export default function DummyEffectForm({
  onChange,
  effect,
}: {
  onChange: (updatedState: Effect) => void;
  effect: Effect;
}) {
  
  return <></>;
}
