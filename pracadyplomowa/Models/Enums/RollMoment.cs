namespace pracadyplomowa.Models.Enums
{
    public enum RollMoment //used to determine whether value effect blueprint should roll once only on cast, or every time effect gets resolved.
    {
        OnCast = 0,
        OnResolve = 1
    }
}